{
  "devDependencies": {
    "install": "^0.13.0",
    "npm": "^9.8.1",
    "parcel": "^2.9.3",
    "posthtml-include": "^1.7.4"
  },
  "posthtml": {
    "plugins": {
      "posthtml-include": {}
    }
  },
  "alias": {
    "web_lib": "./web_lib/"
  },
  "@parcel/transformer-css": {
    "drafts": {
    }
  },
  "//" : "using --no-hmr as a workaround for parcel crashing when rebuilding",
  "scripts": {
    "dev": "parcel --no-cache --no-hmr index.html",
    "build": "parcel build index.html"
  }
}